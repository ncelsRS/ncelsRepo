@model Guid[]

@{
     //Layout = "~/Views/Shared/_Layout.cshtml";
}


<script>

    function docAdminPage1onClick() {
        var itemId = '@Model[0].ToString()';

        $.ajax({
            url: "/AdministrativeMainDoc/Form?Id=" + itemId,
            //type: "POST",
            success: function (result) {
                // refreshes partial view
                $("#page" + itemId).html(result);
            }
        });
    }
    function docAdminPage2onClick() {
        var itemId = '@Model[0].ToString()';
        $.ajax({
            url: "/Task/Details?Id=" + itemId,
            //type: "POST",
            success: function (result) {
                // refreshes partial view
                $("#page" + itemId).html(result);
            }
        });
    }
    function docAdminPage4onClick() {
        var itemId = '@Model[0].ToString()';
        $.ajax({
            url: "/History/Index?Id=" + itemId,
            //type: "POST",
            success: function (result) {
                // refreshes partial view
                $("#page" + itemId).html(result);
            }
        });
    }
    function docAdminPage5onClick() {
        var itemId = '@Model[0].ToString()';
        $.ajax({
            url: "/History/ExtensionExecution?Id=" + itemId,
            //type: "POST",
            success: function (result) {
                // refreshes partial view
                $("#page" + itemId).html(result);
            }
        });
    }
</script>

<div class="pwTabContent k-block">

    <form id="@("admDocForm"+ Model[0])">
       
        <div id="@("loader"+ Model[0])">
        </div>
        <table class="pwTable">
            <tr class="pwContent">
                <td class="pwContentLeft k-block">
                    
                    <a onclick="docAdminPage1onClick()" id="@("docPage1" + Model[0])" class="pwBtn pwBtn-5 pwBtn-5a icon-cart"><span><img src="../../Content/images/card.png"/></span></a>
                    <a onclick="docAdminPage2onClick()" id="@("docPage2" + Model[0])" class="pwBtn pwBtn-5 pwBtn-5a icon-cart-2"><span><img src="../../Content/images/tasks.png" /></span></a>
                    <a onclick="docAdminPage5onClick()" id="@("docPage5" + Model[0])" class="pwBtn pwBtn-5 pwBtn-5a icon-cart-6"><span><img src="../../Content/images/exec.png" /></span></a>
                    <a onclick="docAdminPage4onClick()" id="@("docPage4" + Model[0])" class="pwBtn pwBtn-5 pwBtn-5a icon-cart-4"><span><img src="../../Content/images/history.png" /></span></a>
                </td>
                <td class="pwContentRight k-block" id="@("pwContentRightId"+ Model[0])">
                    <div class="pwPageScrollable">
                        <div id="@("page" + Model[0])">
                            @Html.Partial("Form",Model)
                        </div>
                    </div>
                </td>
            </tr>
            <tr>
                <td class="pwStatusbar k-block" colspan="2">
                    <div class="pwFloat">
                        @PW.Ncels.Database.Recources.Messages.Property_Приказ_350__00
                    </div>
                    <div class="pwFloat pwLine">

                    </div>
                    <div class="pwFloat">
                        <span id="@("pwStatusbarState" + Model[0])"></span>
                    </div>
                    <div class="pwFloat pwLine">

                    </div>
                    <div class="pwFloat">
                        
                    </div>
                    <div class="pwFloat pwLine">

                    </div>
                    <div class="pwFloat">
                        
                    </div>
                    <div class="pwFloat pwLine">

                    </div>
                </td>
            </tr>
        </table>
    </form>
</div>
<script>
    InitializeSizeAdministrativeMain('@Model[0].ToString()');
</script>

